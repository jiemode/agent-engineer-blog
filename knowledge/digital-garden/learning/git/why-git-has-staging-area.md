# 为什么 Git 要有暂存区？

刚接触 Git 的时候，我一直觉得暂存区很多余。

既然已经修改了文件，为什么不能直接提交？

后来，在学习 Git 工作流的时候，我开始意识到，暂存区并不是为了增加操作步骤，而是为了给开发者保留实验和选择的空间。

我当时猜测，很多修改其实都是实验性的。

开发者只是想试试看效果，而不是立刻把这些修改纳入下一次版本。

后来我才意识到，我把 Git 和 GitHub 混在一起了。

事实上，暂存区根本不是为了 GitHub 存在。

它真正的作用，是让开发者能够自由选择哪些修改进入下一次 Commit。
